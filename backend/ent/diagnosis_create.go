// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/team06/app/ent/diagnosis"
	"github.com/team06/app/ent/doctor"
	"github.com/team06/app/ent/patient"
	"github.com/team06/app/ent/treatmenttype"
)

// DiagnosisCreate is the builder for creating a Diagnosis entity.
type DiagnosisCreate struct {
	config
	mutation *DiagnosisMutation
	hooks    []Hook
}

// SetSymptom sets the "symptom" field.
func (dc *DiagnosisCreate) SetSymptom(s string) *DiagnosisCreate {
	dc.mutation.SetSymptom(s)
	return dc
}

// SetOpinionresult sets the "Opinionresult" field.
func (dc *DiagnosisCreate) SetOpinionresult(s string) *DiagnosisCreate {
	dc.mutation.SetOpinionresult(s)
	return dc
}

// SetNote sets the "note" field.
func (dc *DiagnosisCreate) SetNote(s string) *DiagnosisCreate {
	dc.mutation.SetNote(s)
	return dc
}

// SetDiagnosisDate sets the "diagnosisDate" field.
func (dc *DiagnosisCreate) SetDiagnosisDate(t time.Time) *DiagnosisCreate {
	dc.mutation.SetDiagnosisDate(t)
	return dc
}

// SetNillableDiagnosisDate sets the "diagnosisDate" field if the given value is not nil.
func (dc *DiagnosisCreate) SetNillableDiagnosisDate(t *time.Time) *DiagnosisCreate {
	if t != nil {
		dc.SetDiagnosisDate(*t)
	}
	return dc
}

// SetDoctorNameID sets the "Doctor_name" edge to the Doctor entity by ID.
func (dc *DiagnosisCreate) SetDoctorNameID(id int) *DiagnosisCreate {
	dc.mutation.SetDoctorNameID(id)
	return dc
}

// SetNillableDoctorNameID sets the "Doctor_name" edge to the Doctor entity by ID if the given value is not nil.
func (dc *DiagnosisCreate) SetNillableDoctorNameID(id *int) *DiagnosisCreate {
	if id != nil {
		dc = dc.SetDoctorNameID(*id)
	}
	return dc
}

// SetDoctorName sets the "Doctor_name" edge to the Doctor entity.
func (dc *DiagnosisCreate) SetDoctorName(d *Doctor) *DiagnosisCreate {
	return dc.SetDoctorNameID(d.ID)
}

// SetPatientID sets the "Patient" edge to the Patient entity by ID.
func (dc *DiagnosisCreate) SetPatientID(id int) *DiagnosisCreate {
	dc.mutation.SetPatientID(id)
	return dc
}

// SetNillablePatientID sets the "Patient" edge to the Patient entity by ID if the given value is not nil.
func (dc *DiagnosisCreate) SetNillablePatientID(id *int) *DiagnosisCreate {
	if id != nil {
		dc = dc.SetPatientID(*id)
	}
	return dc
}

// SetPatient sets the "Patient" edge to the Patient entity.
func (dc *DiagnosisCreate) SetPatient(p *Patient) *DiagnosisCreate {
	return dc.SetPatientID(p.ID)
}

// SetTypeID sets the "type" edge to the TreatmentType entity by ID.
func (dc *DiagnosisCreate) SetTypeID(id int) *DiagnosisCreate {
	dc.mutation.SetTypeID(id)
	return dc
}

// SetNillableTypeID sets the "type" edge to the TreatmentType entity by ID if the given value is not nil.
func (dc *DiagnosisCreate) SetNillableTypeID(id *int) *DiagnosisCreate {
	if id != nil {
		dc = dc.SetTypeID(*id)
	}
	return dc
}

// SetType sets the "type" edge to the TreatmentType entity.
func (dc *DiagnosisCreate) SetType(t *TreatmentType) *DiagnosisCreate {
	return dc.SetTypeID(t.ID)
}

// Mutation returns the DiagnosisMutation object of the builder.
func (dc *DiagnosisCreate) Mutation() *DiagnosisMutation {
	return dc.mutation
}

// Save creates the Diagnosis in the database.
func (dc *DiagnosisCreate) Save(ctx context.Context) (*Diagnosis, error) {
	var (
		err  error
		node *Diagnosis
	)
	dc.defaults()
	if len(dc.hooks) == 0 {
		if err = dc.check(); err != nil {
			return nil, err
		}
		node, err = dc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*DiagnosisMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = dc.check(); err != nil {
				return nil, err
			}
			dc.mutation = mutation
			node, err = dc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(dc.hooks) - 1; i >= 0; i-- {
			mut = dc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, dc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (dc *DiagnosisCreate) SaveX(ctx context.Context) *Diagnosis {
	v, err := dc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (dc *DiagnosisCreate) defaults() {
	if _, ok := dc.mutation.DiagnosisDate(); !ok {
		v := diagnosis.DefaultDiagnosisDate()
		dc.mutation.SetDiagnosisDate(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (dc *DiagnosisCreate) check() error {
	if _, ok := dc.mutation.Symptom(); !ok {
		return &ValidationError{Name: "symptom", err: errors.New("ent: missing required field \"symptom\"")}
	}
	if v, ok := dc.mutation.Symptom(); ok {
		if err := diagnosis.SymptomValidator(v); err != nil {
			return &ValidationError{Name: "symptom", err: fmt.Errorf("ent: validator failed for field \"symptom\": %w", err)}
		}
	}
	if _, ok := dc.mutation.Opinionresult(); !ok {
		return &ValidationError{Name: "Opinionresult", err: errors.New("ent: missing required field \"Opinionresult\"")}
	}
	if v, ok := dc.mutation.Opinionresult(); ok {
		if err := diagnosis.OpinionresultValidator(v); err != nil {
			return &ValidationError{Name: "Opinionresult", err: fmt.Errorf("ent: validator failed for field \"Opinionresult\": %w", err)}
		}
	}
	if _, ok := dc.mutation.Note(); !ok {
		return &ValidationError{Name: "note", err: errors.New("ent: missing required field \"note\"")}
	}
	if v, ok := dc.mutation.Note(); ok {
		if err := diagnosis.NoteValidator(v); err != nil {
			return &ValidationError{Name: "note", err: fmt.Errorf("ent: validator failed for field \"note\": %w", err)}
		}
	}
	if _, ok := dc.mutation.DiagnosisDate(); !ok {
		return &ValidationError{Name: "diagnosisDate", err: errors.New("ent: missing required field \"diagnosisDate\"")}
	}
	return nil
}

func (dc *DiagnosisCreate) sqlSave(ctx context.Context) (*Diagnosis, error) {
	_node, _spec := dc.createSpec()
	if err := sqlgraph.CreateNode(ctx, dc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (dc *DiagnosisCreate) createSpec() (*Diagnosis, *sqlgraph.CreateSpec) {
	var (
		_node = &Diagnosis{config: dc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: diagnosis.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: diagnosis.FieldID,
			},
		}
	)
	if value, ok := dc.mutation.Symptom(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: diagnosis.FieldSymptom,
		})
		_node.Symptom = value
	}
	if value, ok := dc.mutation.Opinionresult(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: diagnosis.FieldOpinionresult,
		})
		_node.Opinionresult = value
	}
	if value, ok := dc.mutation.Note(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: diagnosis.FieldNote,
		})
		_node.Note = value
	}
	if value, ok := dc.mutation.DiagnosisDate(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: diagnosis.FieldDiagnosisDate,
		})
		_node.DiagnosisDate = value
	}
	if nodes := dc.mutation.DoctorNameIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   diagnosis.DoctorNameTable,
			Columns: []string{diagnosis.DoctorNameColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: doctor.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := dc.mutation.PatientIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   diagnosis.PatientTable,
			Columns: []string{diagnosis.PatientColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: patient.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := dc.mutation.TypeIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   diagnosis.TypeTable,
			Columns: []string{diagnosis.TypeColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: treatmenttype.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// DiagnosisCreateBulk is the builder for creating many Diagnosis entities in bulk.
type DiagnosisCreateBulk struct {
	config
	builders []*DiagnosisCreate
}

// Save creates the Diagnosis entities in the database.
func (dcb *DiagnosisCreateBulk) Save(ctx context.Context) ([]*Diagnosis, error) {
	specs := make([]*sqlgraph.CreateSpec, len(dcb.builders))
	nodes := make([]*Diagnosis, len(dcb.builders))
	mutators := make([]Mutator, len(dcb.builders))
	for i := range dcb.builders {
		func(i int, root context.Context) {
			builder := dcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DiagnosisMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, dcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, dcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, dcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (dcb *DiagnosisCreateBulk) SaveX(ctx context.Context) []*Diagnosis {
	v, err := dcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
