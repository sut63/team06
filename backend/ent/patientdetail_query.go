// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"github.com/facebook/ent/dialect/sql"
	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/team06/app/ent/bloodtype"
	"github.com/team06/app/ent/gender"
	"github.com/team06/app/ent/patient"
	"github.com/team06/app/ent/patientdetail"
	"github.com/team06/app/ent/predicate"
	"github.com/team06/app/ent/prefix"
)

// PatientDetailQuery is the builder for querying PatientDetail entities.
type PatientDetailQuery struct {
	config
	limit      *int
	offset     *int
	order      []OrderFunc
	fields     []string
	predicates []predicate.PatientDetail
	// eager-loading edges.
	withPrefix    *PrefixQuery
	withGender    *GenderQuery
	withBloodtype *BloodTypeQuery
	withPatient   *PatientQuery
	withFKs       bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PatientDetailQuery builder.
func (pdq *PatientDetailQuery) Where(ps ...predicate.PatientDetail) *PatientDetailQuery {
	pdq.predicates = append(pdq.predicates, ps...)
	return pdq
}

// Limit adds a limit step to the query.
func (pdq *PatientDetailQuery) Limit(limit int) *PatientDetailQuery {
	pdq.limit = &limit
	return pdq
}

// Offset adds an offset step to the query.
func (pdq *PatientDetailQuery) Offset(offset int) *PatientDetailQuery {
	pdq.offset = &offset
	return pdq
}

// Order adds an order step to the query.
func (pdq *PatientDetailQuery) Order(o ...OrderFunc) *PatientDetailQuery {
	pdq.order = append(pdq.order, o...)
	return pdq
}

// QueryPrefix chains the current query on the "prefix" edge.
func (pdq *PatientDetailQuery) QueryPrefix() *PrefixQuery {
	query := &PrefixQuery{config: pdq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patientdetail.Table, patientdetail.FieldID, selector),
			sqlgraph.To(prefix.Table, prefix.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patientdetail.PrefixTable, patientdetail.PrefixColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGender chains the current query on the "gender" edge.
func (pdq *PatientDetailQuery) QueryGender() *GenderQuery {
	query := &GenderQuery{config: pdq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patientdetail.Table, patientdetail.FieldID, selector),
			sqlgraph.To(gender.Table, gender.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patientdetail.GenderTable, patientdetail.GenderColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBloodtype chains the current query on the "bloodtype" edge.
func (pdq *PatientDetailQuery) QueryBloodtype() *BloodTypeQuery {
	query := &BloodTypeQuery{config: pdq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patientdetail.Table, patientdetail.FieldID, selector),
			sqlgraph.To(bloodtype.Table, bloodtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patientdetail.BloodtypeTable, patientdetail.BloodtypeColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatient chains the current query on the "patient" edge.
func (pdq *PatientDetailQuery) QueryPatient() *PatientQuery {
	query := &PatientQuery{config: pdq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patientdetail.Table, patientdetail.FieldID, selector),
			sqlgraph.To(patient.Table, patient.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patientdetail.PatientTable, patientdetail.PatientColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PatientDetail entity from the query.
// Returns a *NotFoundError when no PatientDetail was found.
func (pdq *PatientDetailQuery) First(ctx context.Context) (*PatientDetail, error) {
	nodes, err := pdq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{patientdetail.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pdq *PatientDetailQuery) FirstX(ctx context.Context) *PatientDetail {
	node, err := pdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PatientDetail ID from the query.
// Returns a *NotFoundError when no PatientDetail ID was found.
func (pdq *PatientDetailQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pdq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{patientdetail.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pdq *PatientDetailQuery) FirstIDX(ctx context.Context) int {
	id, err := pdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PatientDetail entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one PatientDetail entity is not found.
// Returns a *NotFoundError when no PatientDetail entities are found.
func (pdq *PatientDetailQuery) Only(ctx context.Context) (*PatientDetail, error) {
	nodes, err := pdq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{patientdetail.Label}
	default:
		return nil, &NotSingularError{patientdetail.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pdq *PatientDetailQuery) OnlyX(ctx context.Context) *PatientDetail {
	node, err := pdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PatientDetail ID in the query.
// Returns a *NotSingularError when exactly one PatientDetail ID is not found.
// Returns a *NotFoundError when no entities are found.
func (pdq *PatientDetailQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pdq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = &NotSingularError{patientdetail.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pdq *PatientDetailQuery) OnlyIDX(ctx context.Context) int {
	id, err := pdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PatientDetails.
func (pdq *PatientDetailQuery) All(ctx context.Context) ([]*PatientDetail, error) {
	if err := pdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return pdq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (pdq *PatientDetailQuery) AllX(ctx context.Context) []*PatientDetail {
	nodes, err := pdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PatientDetail IDs.
func (pdq *PatientDetailQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := pdq.Select(patientdetail.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pdq *PatientDetailQuery) IDsX(ctx context.Context) []int {
	ids, err := pdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pdq *PatientDetailQuery) Count(ctx context.Context) (int, error) {
	if err := pdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return pdq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (pdq *PatientDetailQuery) CountX(ctx context.Context) int {
	count, err := pdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pdq *PatientDetailQuery) Exist(ctx context.Context) (bool, error) {
	if err := pdq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return pdq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (pdq *PatientDetailQuery) ExistX(ctx context.Context) bool {
	exist, err := pdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PatientDetailQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pdq *PatientDetailQuery) Clone() *PatientDetailQuery {
	if pdq == nil {
		return nil
	}
	return &PatientDetailQuery{
		config:        pdq.config,
		limit:         pdq.limit,
		offset:        pdq.offset,
		order:         append([]OrderFunc{}, pdq.order...),
		predicates:    append([]predicate.PatientDetail{}, pdq.predicates...),
		withPrefix:    pdq.withPrefix.Clone(),
		withGender:    pdq.withGender.Clone(),
		withBloodtype: pdq.withBloodtype.Clone(),
		withPatient:   pdq.withPatient.Clone(),
		// clone intermediate query.
		sql:  pdq.sql.Clone(),
		path: pdq.path,
	}
}

// WithPrefix tells the query-builder to eager-load the nodes that are connected to
// the "prefix" edge. The optional arguments are used to configure the query builder of the edge.
func (pdq *PatientDetailQuery) WithPrefix(opts ...func(*PrefixQuery)) *PatientDetailQuery {
	query := &PrefixQuery{config: pdq.config}
	for _, opt := range opts {
		opt(query)
	}
	pdq.withPrefix = query
	return pdq
}

// WithGender tells the query-builder to eager-load the nodes that are connected to
// the "gender" edge. The optional arguments are used to configure the query builder of the edge.
func (pdq *PatientDetailQuery) WithGender(opts ...func(*GenderQuery)) *PatientDetailQuery {
	query := &GenderQuery{config: pdq.config}
	for _, opt := range opts {
		opt(query)
	}
	pdq.withGender = query
	return pdq
}

// WithBloodtype tells the query-builder to eager-load the nodes that are connected to
// the "bloodtype" edge. The optional arguments are used to configure the query builder of the edge.
func (pdq *PatientDetailQuery) WithBloodtype(opts ...func(*BloodTypeQuery)) *PatientDetailQuery {
	query := &BloodTypeQuery{config: pdq.config}
	for _, opt := range opts {
		opt(query)
	}
	pdq.withBloodtype = query
	return pdq
}

// WithPatient tells the query-builder to eager-load the nodes that are connected to
// the "patient" edge. The optional arguments are used to configure the query builder of the edge.
func (pdq *PatientDetailQuery) WithPatient(opts ...func(*PatientQuery)) *PatientDetailQuery {
	query := &PatientQuery{config: pdq.config}
	for _, opt := range opts {
		opt(query)
	}
	pdq.withPatient = query
	return pdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (pdq *PatientDetailQuery) GroupBy(field string, fields ...string) *PatientDetailGroupBy {
	group := &PatientDetailGroupBy{config: pdq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := pdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return pdq.sqlQuery(), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (pdq *PatientDetailQuery) Select(field string, fields ...string) *PatientDetailSelect {
	pdq.fields = append([]string{field}, fields...)
	return &PatientDetailSelect{PatientDetailQuery: pdq}
}

func (pdq *PatientDetailQuery) prepareQuery(ctx context.Context) error {
	for _, f := range pdq.fields {
		if !patientdetail.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pdq.path != nil {
		prev, err := pdq.path(ctx)
		if err != nil {
			return err
		}
		pdq.sql = prev
	}
	return nil
}

func (pdq *PatientDetailQuery) sqlAll(ctx context.Context) ([]*PatientDetail, error) {
	var (
		nodes       = []*PatientDetail{}
		withFKs     = pdq.withFKs
		_spec       = pdq.querySpec()
		loadedTypes = [4]bool{
			pdq.withPrefix != nil,
			pdq.withGender != nil,
			pdq.withBloodtype != nil,
			pdq.withPatient != nil,
		}
	)
	if pdq.withPrefix != nil || pdq.withGender != nil || pdq.withBloodtype != nil || pdq.withPatient != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, patientdetail.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &PatientDetail{config: pdq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, pdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := pdq.withPrefix; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PatientDetail)
		for i := range nodes {
			if fk := nodes[i].prefix_patient_details; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(prefix.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "prefix_patient_details" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Prefix = n
			}
		}
	}

	if query := pdq.withGender; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PatientDetail)
		for i := range nodes {
			if fk := nodes[i].gender_patient_details; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(gender.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "gender_patient_details" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Gender = n
			}
		}
	}

	if query := pdq.withBloodtype; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PatientDetail)
		for i := range nodes {
			if fk := nodes[i].blood_type_patient_details; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(bloodtype.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "blood_type_patient_details" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Bloodtype = n
			}
		}
	}

	if query := pdq.withPatient; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PatientDetail)
		for i := range nodes {
			if fk := nodes[i].patient_patient_details; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(patient.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "patient_patient_details" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Patient = n
			}
		}
	}

	return nodes, nil
}

func (pdq *PatientDetailQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pdq.querySpec()
	return sqlgraph.CountNodes(ctx, pdq.driver, _spec)
}

func (pdq *PatientDetailQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := pdq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %v", err)
	}
	return n > 0, nil
}

func (pdq *PatientDetailQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   patientdetail.Table,
			Columns: patientdetail.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: patientdetail.FieldID,
			},
		},
		From:   pdq.sql,
		Unique: true,
	}
	if fields := pdq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, patientdetail.FieldID)
		for i := range fields {
			if fields[i] != patientdetail.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pdq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pdq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector, patientdetail.ValidColumn)
			}
		}
	}
	return _spec
}

func (pdq *PatientDetailQuery) sqlQuery() *sql.Selector {
	builder := sql.Dialect(pdq.driver.Dialect())
	t1 := builder.Table(patientdetail.Table)
	selector := builder.Select(t1.Columns(patientdetail.Columns...)...).From(t1)
	if pdq.sql != nil {
		selector = pdq.sql
		selector.Select(selector.Columns(patientdetail.Columns...)...)
	}
	for _, p := range pdq.predicates {
		p(selector)
	}
	for _, p := range pdq.order {
		p(selector, patientdetail.ValidColumn)
	}
	if offset := pdq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pdq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PatientDetailGroupBy is the group-by builder for PatientDetail entities.
type PatientDetailGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pdgb *PatientDetailGroupBy) Aggregate(fns ...AggregateFunc) *PatientDetailGroupBy {
	pdgb.fns = append(pdgb.fns, fns...)
	return pdgb
}

// Scan applies the group-by query and scans the result into the given value.
func (pdgb *PatientDetailGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pdgb.path(ctx)
	if err != nil {
		return err
	}
	pdgb.sql = query
	return pdgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := pdgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (pdgb *PatientDetailGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(pdgb.fields) > 1 {
		return nil, errors.New("ent: PatientDetailGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := pdgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) StringsX(ctx context.Context) []string {
	v, err := pdgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pdgb *PatientDetailGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pdgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = fmt.Errorf("ent: PatientDetailGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) StringX(ctx context.Context) string {
	v, err := pdgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (pdgb *PatientDetailGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(pdgb.fields) > 1 {
		return nil, errors.New("ent: PatientDetailGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := pdgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) IntsX(ctx context.Context) []int {
	v, err := pdgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pdgb *PatientDetailGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pdgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = fmt.Errorf("ent: PatientDetailGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) IntX(ctx context.Context) int {
	v, err := pdgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (pdgb *PatientDetailGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(pdgb.fields) > 1 {
		return nil, errors.New("ent: PatientDetailGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := pdgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := pdgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pdgb *PatientDetailGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pdgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = fmt.Errorf("ent: PatientDetailGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) Float64X(ctx context.Context) float64 {
	v, err := pdgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (pdgb *PatientDetailGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(pdgb.fields) > 1 {
		return nil, errors.New("ent: PatientDetailGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := pdgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := pdgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pdgb *PatientDetailGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pdgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = fmt.Errorf("ent: PatientDetailGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pdgb *PatientDetailGroupBy) BoolX(ctx context.Context) bool {
	v, err := pdgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pdgb *PatientDetailGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pdgb.fields {
		if !patientdetail.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pdgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pdgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pdgb *PatientDetailGroupBy) sqlQuery() *sql.Selector {
	selector := pdgb.sql
	columns := make([]string, 0, len(pdgb.fields)+len(pdgb.fns))
	columns = append(columns, pdgb.fields...)
	for _, fn := range pdgb.fns {
		columns = append(columns, fn(selector, patientdetail.ValidColumn))
	}
	return selector.Select(columns...).GroupBy(pdgb.fields...)
}

// PatientDetailSelect is the builder for selecting fields of PatientDetail entities.
type PatientDetailSelect struct {
	*PatientDetailQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (pds *PatientDetailSelect) Scan(ctx context.Context, v interface{}) error {
	if err := pds.prepareQuery(ctx); err != nil {
		return err
	}
	pds.sql = pds.PatientDetailQuery.sqlQuery()
	return pds.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pds *PatientDetailSelect) ScanX(ctx context.Context, v interface{}) {
	if err := pds.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (pds *PatientDetailSelect) Strings(ctx context.Context) ([]string, error) {
	if len(pds.fields) > 1 {
		return nil, errors.New("ent: PatientDetailSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := pds.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pds *PatientDetailSelect) StringsX(ctx context.Context) []string {
	v, err := pds.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (pds *PatientDetailSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pds.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = fmt.Errorf("ent: PatientDetailSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pds *PatientDetailSelect) StringX(ctx context.Context) string {
	v, err := pds.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (pds *PatientDetailSelect) Ints(ctx context.Context) ([]int, error) {
	if len(pds.fields) > 1 {
		return nil, errors.New("ent: PatientDetailSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := pds.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pds *PatientDetailSelect) IntsX(ctx context.Context) []int {
	v, err := pds.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (pds *PatientDetailSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pds.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = fmt.Errorf("ent: PatientDetailSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pds *PatientDetailSelect) IntX(ctx context.Context) int {
	v, err := pds.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (pds *PatientDetailSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(pds.fields) > 1 {
		return nil, errors.New("ent: PatientDetailSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := pds.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pds *PatientDetailSelect) Float64sX(ctx context.Context) []float64 {
	v, err := pds.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (pds *PatientDetailSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pds.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = fmt.Errorf("ent: PatientDetailSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pds *PatientDetailSelect) Float64X(ctx context.Context) float64 {
	v, err := pds.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (pds *PatientDetailSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(pds.fields) > 1 {
		return nil, errors.New("ent: PatientDetailSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := pds.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pds *PatientDetailSelect) BoolsX(ctx context.Context) []bool {
	v, err := pds.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (pds *PatientDetailSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pds.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientdetail.Label}
	default:
		err = fmt.Errorf("ent: PatientDetailSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pds *PatientDetailSelect) BoolX(ctx context.Context) bool {
	v, err := pds.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pds *PatientDetailSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pds.sqlQuery().Query()
	if err := pds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pds *PatientDetailSelect) sqlQuery() sql.Querier {
	selector := pds.sql
	selector.Select(selector.Columns(pds.fields...)...)
	return selector
}
