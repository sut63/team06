// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"github.com/facebook/ent/dialect/sql"
	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/team06/app/ent/appointmentresults"
	"github.com/team06/app/ent/bloodtype"
	"github.com/team06/app/ent/diagnosis"
	"github.com/team06/app/ent/gender"
	"github.com/team06/app/ent/medicalprocedure"
	"github.com/team06/app/ent/patient"
	"github.com/team06/app/ent/predicate"
	"github.com/team06/app/ent/prefix"
	"github.com/team06/app/ent/righttotreatment"
	"github.com/team06/app/ent/triageresult"
)

// PatientQuery is the builder for querying Patient entities.
type PatientQuery struct {
	config
	limit      *int
	offset     *int
	order      []OrderFunc
	fields     []string
	predicates []predicate.Patient
	// eager-loading edges.
	withPrefix                      *PrefixQuery
	withGender                      *GenderQuery
	withBloodtype                   *BloodTypeQuery
	withTriageResult                *TriageResultQuery
	withPatientToAppointmentResults *AppointmentResultsQuery
	withPatientToMedicalProcedure   *MedicalProcedureQuery
	withPatientToRightToTreatment   *RightToTreatmentQuery
	withPatientToDiagnosis          *DiagnosisQuery
	withFKs                         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PatientQuery builder.
func (pq *PatientQuery) Where(ps ...predicate.Patient) *PatientQuery {
	pq.predicates = append(pq.predicates, ps...)
	return pq
}

// Limit adds a limit step to the query.
func (pq *PatientQuery) Limit(limit int) *PatientQuery {
	pq.limit = &limit
	return pq
}

// Offset adds an offset step to the query.
func (pq *PatientQuery) Offset(offset int) *PatientQuery {
	pq.offset = &offset
	return pq
}

// Order adds an order step to the query.
func (pq *PatientQuery) Order(o ...OrderFunc) *PatientQuery {
	pq.order = append(pq.order, o...)
	return pq
}

// QueryPrefix chains the current query on the "prefix" edge.
func (pq *PatientQuery) QueryPrefix() *PrefixQuery {
	query := &PrefixQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(prefix.Table, prefix.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patient.PrefixTable, patient.PrefixColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGender chains the current query on the "gender" edge.
func (pq *PatientQuery) QueryGender() *GenderQuery {
	query := &GenderQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(gender.Table, gender.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patient.GenderTable, patient.GenderColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBloodtype chains the current query on the "bloodtype" edge.
func (pq *PatientQuery) QueryBloodtype() *BloodTypeQuery {
	query := &BloodTypeQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(bloodtype.Table, bloodtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patient.BloodtypeTable, patient.BloodtypeColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTriageResult chains the current query on the "triageResult" edge.
func (pq *PatientQuery) QueryTriageResult() *TriageResultQuery {
	query := &TriageResultQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(triageresult.Table, triageresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.TriageResultTable, patient.TriageResultColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientToAppointmentResults chains the current query on the "PatientToAppointmentResults" edge.
func (pq *PatientQuery) QueryPatientToAppointmentResults() *AppointmentResultsQuery {
	query := &AppointmentResultsQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(appointmentresults.Table, appointmentresults.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.PatientToAppointmentResultsTable, patient.PatientToAppointmentResultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientToMedicalProcedure chains the current query on the "PatientToMedicalProcedure" edge.
func (pq *PatientQuery) QueryPatientToMedicalProcedure() *MedicalProcedureQuery {
	query := &MedicalProcedureQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(medicalprocedure.Table, medicalprocedure.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.PatientToMedicalProcedureTable, patient.PatientToMedicalProcedureColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientToRightToTreatment chains the current query on the "PatientToRightToTreatment" edge.
func (pq *PatientQuery) QueryPatientToRightToTreatment() *RightToTreatmentQuery {
	query := &RightToTreatmentQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(righttotreatment.Table, righttotreatment.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.PatientToRightToTreatmentTable, patient.PatientToRightToTreatmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientToDiagnosis chains the current query on the "PatientToDiagnosis" edge.
func (pq *PatientQuery) QueryPatientToDiagnosis() *DiagnosisQuery {
	query := &DiagnosisQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(diagnosis.Table, diagnosis.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.PatientToDiagnosisTable, patient.PatientToDiagnosisColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Patient entity from the query.
// Returns a *NotFoundError when no Patient was found.
func (pq *PatientQuery) First(ctx context.Context) (*Patient, error) {
	nodes, err := pq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{patient.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pq *PatientQuery) FirstX(ctx context.Context) *Patient {
	node, err := pq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Patient ID from the query.
// Returns a *NotFoundError when no Patient ID was found.
func (pq *PatientQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{patient.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pq *PatientQuery) FirstIDX(ctx context.Context) int {
	id, err := pq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Patient entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one Patient entity is not found.
// Returns a *NotFoundError when no Patient entities are found.
func (pq *PatientQuery) Only(ctx context.Context) (*Patient, error) {
	nodes, err := pq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{patient.Label}
	default:
		return nil, &NotSingularError{patient.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pq *PatientQuery) OnlyX(ctx context.Context) *Patient {
	node, err := pq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Patient ID in the query.
// Returns a *NotSingularError when exactly one Patient ID is not found.
// Returns a *NotFoundError when no entities are found.
func (pq *PatientQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = &NotSingularError{patient.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pq *PatientQuery) OnlyIDX(ctx context.Context) int {
	id, err := pq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Patients.
func (pq *PatientQuery) All(ctx context.Context) ([]*Patient, error) {
	if err := pq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return pq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (pq *PatientQuery) AllX(ctx context.Context) []*Patient {
	nodes, err := pq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Patient IDs.
func (pq *PatientQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := pq.Select(patient.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pq *PatientQuery) IDsX(ctx context.Context) []int {
	ids, err := pq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pq *PatientQuery) Count(ctx context.Context) (int, error) {
	if err := pq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return pq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (pq *PatientQuery) CountX(ctx context.Context) int {
	count, err := pq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pq *PatientQuery) Exist(ctx context.Context) (bool, error) {
	if err := pq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return pq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (pq *PatientQuery) ExistX(ctx context.Context) bool {
	exist, err := pq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PatientQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pq *PatientQuery) Clone() *PatientQuery {
	if pq == nil {
		return nil
	}
	return &PatientQuery{
		config:                          pq.config,
		limit:                           pq.limit,
		offset:                          pq.offset,
		order:                           append([]OrderFunc{}, pq.order...),
		predicates:                      append([]predicate.Patient{}, pq.predicates...),
		withPrefix:                      pq.withPrefix.Clone(),
		withGender:                      pq.withGender.Clone(),
		withBloodtype:                   pq.withBloodtype.Clone(),
		withTriageResult:                pq.withTriageResult.Clone(),
		withPatientToAppointmentResults: pq.withPatientToAppointmentResults.Clone(),
		withPatientToMedicalProcedure:   pq.withPatientToMedicalProcedure.Clone(),
		withPatientToRightToTreatment:   pq.withPatientToRightToTreatment.Clone(),
		withPatientToDiagnosis:          pq.withPatientToDiagnosis.Clone(),
		// clone intermediate query.
		sql:  pq.sql.Clone(),
		path: pq.path,
	}
}

// WithPrefix tells the query-builder to eager-load the nodes that are connected to
// the "prefix" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPrefix(opts ...func(*PrefixQuery)) *PatientQuery {
	query := &PrefixQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withPrefix = query
	return pq
}

// WithGender tells the query-builder to eager-load the nodes that are connected to
// the "gender" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithGender(opts ...func(*GenderQuery)) *PatientQuery {
	query := &GenderQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withGender = query
	return pq
}

// WithBloodtype tells the query-builder to eager-load the nodes that are connected to
// the "bloodtype" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithBloodtype(opts ...func(*BloodTypeQuery)) *PatientQuery {
	query := &BloodTypeQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withBloodtype = query
	return pq
}

// WithTriageResult tells the query-builder to eager-load the nodes that are connected to
// the "triageResult" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithTriageResult(opts ...func(*TriageResultQuery)) *PatientQuery {
	query := &TriageResultQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withTriageResult = query
	return pq
}

// WithPatientToAppointmentResults tells the query-builder to eager-load the nodes that are connected to
// the "PatientToAppointmentResults" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientToAppointmentResults(opts ...func(*AppointmentResultsQuery)) *PatientQuery {
	query := &AppointmentResultsQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientToAppointmentResults = query
	return pq
}

// WithPatientToMedicalProcedure tells the query-builder to eager-load the nodes that are connected to
// the "PatientToMedicalProcedure" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientToMedicalProcedure(opts ...func(*MedicalProcedureQuery)) *PatientQuery {
	query := &MedicalProcedureQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientToMedicalProcedure = query
	return pq
}

// WithPatientToRightToTreatment tells the query-builder to eager-load the nodes that are connected to
// the "PatientToRightToTreatment" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientToRightToTreatment(opts ...func(*RightToTreatmentQuery)) *PatientQuery {
	query := &RightToTreatmentQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientToRightToTreatment = query
	return pq
}

// WithPatientToDiagnosis tells the query-builder to eager-load the nodes that are connected to
// the "PatientToDiagnosis" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientToDiagnosis(opts ...func(*DiagnosisQuery)) *PatientQuery {
	query := &DiagnosisQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientToDiagnosis = query
	return pq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PersonalID string `json:"personalID,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Patient.Query().
//		GroupBy(patient.FieldPersonalID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (pq *PatientQuery) GroupBy(field string, fields ...string) *PatientGroupBy {
	group := &PatientGroupBy{config: pq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return pq.sqlQuery(), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PersonalID string `json:"personalID,omitempty"`
//	}
//
//	client.Patient.Query().
//		Select(patient.FieldPersonalID).
//		Scan(ctx, &v)
//
func (pq *PatientQuery) Select(field string, fields ...string) *PatientSelect {
	pq.fields = append([]string{field}, fields...)
	return &PatientSelect{PatientQuery: pq}
}

func (pq *PatientQuery) prepareQuery(ctx context.Context) error {
	for _, f := range pq.fields {
		if !patient.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pq.path != nil {
		prev, err := pq.path(ctx)
		if err != nil {
			return err
		}
		pq.sql = prev
	}
	return nil
}

func (pq *PatientQuery) sqlAll(ctx context.Context) ([]*Patient, error) {
	var (
		nodes       = []*Patient{}
		withFKs     = pq.withFKs
		_spec       = pq.querySpec()
		loadedTypes = [8]bool{
			pq.withPrefix != nil,
			pq.withGender != nil,
			pq.withBloodtype != nil,
			pq.withTriageResult != nil,
			pq.withPatientToAppointmentResults != nil,
			pq.withPatientToMedicalProcedure != nil,
			pq.withPatientToRightToTreatment != nil,
			pq.withPatientToDiagnosis != nil,
		}
	)
	if pq.withPrefix != nil || pq.withGender != nil || pq.withBloodtype != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, patient.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &Patient{config: pq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, pq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := pq.withPrefix; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*Patient)
		for i := range nodes {
			if fk := nodes[i].prefix_patients; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(prefix.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "prefix_patients" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Prefix = n
			}
		}
	}

	if query := pq.withGender; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*Patient)
		for i := range nodes {
			if fk := nodes[i].gender_patients; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(gender.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "gender_patients" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Gender = n
			}
		}
	}

	if query := pq.withBloodtype; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*Patient)
		for i := range nodes {
			if fk := nodes[i].blood_type_patients; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(bloodtype.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "blood_type_patients" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Bloodtype = n
			}
		}
	}

	if query := pq.withTriageResult; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*Patient)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.TriageResult = []*TriageResult{}
		}
		query.withFKs = true
		query.Where(predicate.TriageResult(func(s *sql.Selector) {
			s.Where(sql.InValues(patient.TriageResultColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.patient_triage_result
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "patient_triage_result" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "patient_triage_result" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.TriageResult = append(node.Edges.TriageResult, n)
		}
	}

	if query := pq.withPatientToAppointmentResults; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*Patient)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.PatientToAppointmentResults = []*AppointmentResults{}
		}
		query.withFKs = true
		query.Where(predicate.AppointmentResults(func(s *sql.Selector) {
			s.Where(sql.InValues(patient.PatientToAppointmentResultsColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.patient_patient_to_appointment_results
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "patient_patient_to_appointment_results" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "patient_patient_to_appointment_results" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.PatientToAppointmentResults = append(node.Edges.PatientToAppointmentResults, n)
		}
	}

	if query := pq.withPatientToMedicalProcedure; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*Patient)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.PatientToMedicalProcedure = []*MedicalProcedure{}
		}
		query.withFKs = true
		query.Where(predicate.MedicalProcedure(func(s *sql.Selector) {
			s.Where(sql.InValues(patient.PatientToMedicalProcedureColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.patient_patient_to_medical_procedure
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "patient_patient_to_medical_procedure" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "patient_patient_to_medical_procedure" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.PatientToMedicalProcedure = append(node.Edges.PatientToMedicalProcedure, n)
		}
	}

	if query := pq.withPatientToRightToTreatment; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*Patient)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.PatientToRightToTreatment = []*RightToTreatment{}
		}
		query.withFKs = true
		query.Where(predicate.RightToTreatment(func(s *sql.Selector) {
			s.Where(sql.InValues(patient.PatientToRightToTreatmentColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.patient_patient_to_right_to_treatment
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "patient_patient_to_right_to_treatment" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "patient_patient_to_right_to_treatment" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.PatientToRightToTreatment = append(node.Edges.PatientToRightToTreatment, n)
		}
	}

	if query := pq.withPatientToDiagnosis; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*Patient)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.PatientToDiagnosis = []*Diagnosis{}
		}
		query.withFKs = true
		query.Where(predicate.Diagnosis(func(s *sql.Selector) {
			s.Where(sql.InValues(patient.PatientToDiagnosisColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.patient_patient_to_diagnosis
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "patient_patient_to_diagnosis" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "patient_patient_to_diagnosis" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.PatientToDiagnosis = append(node.Edges.PatientToDiagnosis, n)
		}
	}

	return nodes, nil
}

func (pq *PatientQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pq.querySpec()
	return sqlgraph.CountNodes(ctx, pq.driver, _spec)
}

func (pq *PatientQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := pq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %v", err)
	}
	return n > 0, nil
}

func (pq *PatientQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   patient.Table,
			Columns: patient.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: patient.FieldID,
			},
		},
		From:   pq.sql,
		Unique: true,
	}
	if fields := pq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, patient.FieldID)
		for i := range fields {
			if fields[i] != patient.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector, patient.ValidColumn)
			}
		}
	}
	return _spec
}

func (pq *PatientQuery) sqlQuery() *sql.Selector {
	builder := sql.Dialect(pq.driver.Dialect())
	t1 := builder.Table(patient.Table)
	selector := builder.Select(t1.Columns(patient.Columns...)...).From(t1)
	if pq.sql != nil {
		selector = pq.sql
		selector.Select(selector.Columns(patient.Columns...)...)
	}
	for _, p := range pq.predicates {
		p(selector)
	}
	for _, p := range pq.order {
		p(selector, patient.ValidColumn)
	}
	if offset := pq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PatientGroupBy is the group-by builder for Patient entities.
type PatientGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pgb *PatientGroupBy) Aggregate(fns ...AggregateFunc) *PatientGroupBy {
	pgb.fns = append(pgb.fns, fns...)
	return pgb
}

// Scan applies the group-by query and scans the result into the given value.
func (pgb *PatientGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pgb.path(ctx)
	if err != nil {
		return err
	}
	pgb.sql = query
	return pgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pgb *PatientGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := pgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (pgb *PatientGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(pgb.fields) > 1 {
		return nil, errors.New("ent: PatientGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := pgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pgb *PatientGroupBy) StringsX(ctx context.Context) []string {
	v, err := pgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pgb *PatientGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = fmt.Errorf("ent: PatientGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pgb *PatientGroupBy) StringX(ctx context.Context) string {
	v, err := pgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (pgb *PatientGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(pgb.fields) > 1 {
		return nil, errors.New("ent: PatientGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := pgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pgb *PatientGroupBy) IntsX(ctx context.Context) []int {
	v, err := pgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pgb *PatientGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = fmt.Errorf("ent: PatientGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pgb *PatientGroupBy) IntX(ctx context.Context) int {
	v, err := pgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (pgb *PatientGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(pgb.fields) > 1 {
		return nil, errors.New("ent: PatientGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := pgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pgb *PatientGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := pgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pgb *PatientGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = fmt.Errorf("ent: PatientGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pgb *PatientGroupBy) Float64X(ctx context.Context) float64 {
	v, err := pgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (pgb *PatientGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(pgb.fields) > 1 {
		return nil, errors.New("ent: PatientGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := pgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pgb *PatientGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := pgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pgb *PatientGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = fmt.Errorf("ent: PatientGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pgb *PatientGroupBy) BoolX(ctx context.Context) bool {
	v, err := pgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pgb *PatientGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pgb.fields {
		if !patient.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pgb *PatientGroupBy) sqlQuery() *sql.Selector {
	selector := pgb.sql
	columns := make([]string, 0, len(pgb.fields)+len(pgb.fns))
	columns = append(columns, pgb.fields...)
	for _, fn := range pgb.fns {
		columns = append(columns, fn(selector, patient.ValidColumn))
	}
	return selector.Select(columns...).GroupBy(pgb.fields...)
}

// PatientSelect is the builder for selecting fields of Patient entities.
type PatientSelect struct {
	*PatientQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ps *PatientSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ps.prepareQuery(ctx); err != nil {
		return err
	}
	ps.sql = ps.PatientQuery.sqlQuery()
	return ps.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (ps *PatientSelect) ScanX(ctx context.Context, v interface{}) {
	if err := ps.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (ps *PatientSelect) Strings(ctx context.Context) ([]string, error) {
	if len(ps.fields) > 1 {
		return nil, errors.New("ent: PatientSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := ps.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (ps *PatientSelect) StringsX(ctx context.Context) []string {
	v, err := ps.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (ps *PatientSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = ps.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = fmt.Errorf("ent: PatientSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (ps *PatientSelect) StringX(ctx context.Context) string {
	v, err := ps.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (ps *PatientSelect) Ints(ctx context.Context) ([]int, error) {
	if len(ps.fields) > 1 {
		return nil, errors.New("ent: PatientSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := ps.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (ps *PatientSelect) IntsX(ctx context.Context) []int {
	v, err := ps.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (ps *PatientSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = ps.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = fmt.Errorf("ent: PatientSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (ps *PatientSelect) IntX(ctx context.Context) int {
	v, err := ps.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (ps *PatientSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(ps.fields) > 1 {
		return nil, errors.New("ent: PatientSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := ps.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (ps *PatientSelect) Float64sX(ctx context.Context) []float64 {
	v, err := ps.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (ps *PatientSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = ps.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = fmt.Errorf("ent: PatientSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (ps *PatientSelect) Float64X(ctx context.Context) float64 {
	v, err := ps.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (ps *PatientSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(ps.fields) > 1 {
		return nil, errors.New("ent: PatientSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := ps.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (ps *PatientSelect) BoolsX(ctx context.Context) []bool {
	v, err := ps.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (ps *PatientSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = ps.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = fmt.Errorf("ent: PatientSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (ps *PatientSelect) BoolX(ctx context.Context) bool {
	v, err := ps.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (ps *PatientSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ps.sqlQuery().Query()
	if err := ps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (ps *PatientSelect) sqlQuery() sql.Querier {
	selector := ps.sql
	selector.Select(selector.Columns(ps.fields...)...)
	return selector
}
