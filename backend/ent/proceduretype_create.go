// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/team06/app/ent/medicalprocedure"
	"github.com/team06/app/ent/proceduretype"
)

// ProcedureTypeCreate is the builder for creating a ProcedureType entity.
type ProcedureTypeCreate struct {
	config
	mutation *ProcedureTypeMutation
	hooks    []Hook
}

// SetProcedureName sets the "ProcedureName" field.
func (ptc *ProcedureTypeCreate) SetProcedureName(s string) *ProcedureTypeCreate {
	ptc.mutation.SetProcedureName(s)
	return ptc
}

// AddProcedureToMedicalProcedureIDs adds the "ProcedureToMedicalProcedure" edge to the MedicalProcedure entity by IDs.
func (ptc *ProcedureTypeCreate) AddProcedureToMedicalProcedureIDs(ids ...int) *ProcedureTypeCreate {
	ptc.mutation.AddProcedureToMedicalProcedureIDs(ids...)
	return ptc
}

// AddProcedureToMedicalProcedure adds the "ProcedureToMedicalProcedure" edges to the MedicalProcedure entity.
func (ptc *ProcedureTypeCreate) AddProcedureToMedicalProcedure(m ...*MedicalProcedure) *ProcedureTypeCreate {
	ids := make([]int, len(m))
	for i := range m {
		ids[i] = m[i].ID
	}
	return ptc.AddProcedureToMedicalProcedureIDs(ids...)
}

// Mutation returns the ProcedureTypeMutation object of the builder.
func (ptc *ProcedureTypeCreate) Mutation() *ProcedureTypeMutation {
	return ptc.mutation
}

// Save creates the ProcedureType in the database.
func (ptc *ProcedureTypeCreate) Save(ctx context.Context) (*ProcedureType, error) {
	var (
		err  error
		node *ProcedureType
	)
	if len(ptc.hooks) == 0 {
		if err = ptc.check(); err != nil {
			return nil, err
		}
		node, err = ptc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ProcedureTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ptc.check(); err != nil {
				return nil, err
			}
			ptc.mutation = mutation
			node, err = ptc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ptc.hooks) - 1; i >= 0; i-- {
			mut = ptc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ptc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ptc *ProcedureTypeCreate) SaveX(ctx context.Context) *ProcedureType {
	v, err := ptc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (ptc *ProcedureTypeCreate) check() error {
	if _, ok := ptc.mutation.ProcedureName(); !ok {
		return &ValidationError{Name: "ProcedureName", err: errors.New("ent: missing required field \"ProcedureName\"")}
	}
	return nil
}

func (ptc *ProcedureTypeCreate) sqlSave(ctx context.Context) (*ProcedureType, error) {
	_node, _spec := ptc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ptc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (ptc *ProcedureTypeCreate) createSpec() (*ProcedureType, *sqlgraph.CreateSpec) {
	var (
		_node = &ProcedureType{config: ptc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: proceduretype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: proceduretype.FieldID,
			},
		}
	)
	if value, ok := ptc.mutation.ProcedureName(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: proceduretype.FieldProcedureName,
		})
		_node.ProcedureName = value
	}
	if nodes := ptc.mutation.ProcedureToMedicalProcedureIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   proceduretype.ProcedureToMedicalProcedureTable,
			Columns: []string{proceduretype.ProcedureToMedicalProcedureColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: medicalprocedure.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ProcedureTypeCreateBulk is the builder for creating many ProcedureType entities in bulk.
type ProcedureTypeCreateBulk struct {
	config
	builders []*ProcedureTypeCreate
}

// Save creates the ProcedureType entities in the database.
func (ptcb *ProcedureTypeCreateBulk) Save(ctx context.Context) ([]*ProcedureType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ptcb.builders))
	nodes := make([]*ProcedureType, len(ptcb.builders))
	mutators := make([]Mutator, len(ptcb.builders))
	for i := range ptcb.builders {
		func(i int, root context.Context) {
			builder := ptcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ProcedureTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ptcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ptcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ptcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ptcb *ProcedureTypeCreateBulk) SaveX(ctx context.Context) []*ProcedureType {
	v, err := ptcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
