// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/team06/app/ent/righttotreatment"
	"github.com/team06/app/ent/righttotreatmenttype"
)

// RightToTreatmentTypeCreate is the builder for creating a RightToTreatmentType entity.
type RightToTreatmentTypeCreate struct {
	config
	mutation *RightToTreatmentTypeMutation
	hooks    []Hook
}

// SetTypeName sets the "TypeName" field.
func (rtttc *RightToTreatmentTypeCreate) SetTypeName(s string) *RightToTreatmentTypeCreate {
	rtttc.mutation.SetTypeName(s)
	return rtttc
}

// AddTypeIDs adds the "type" edge to the RightToTreatment entity by IDs.
func (rtttc *RightToTreatmentTypeCreate) AddTypeIDs(ids ...int) *RightToTreatmentTypeCreate {
	rtttc.mutation.AddTypeIDs(ids...)
	return rtttc
}

// AddType adds the "type" edges to the RightToTreatment entity.
func (rtttc *RightToTreatmentTypeCreate) AddType(r ...*RightToTreatment) *RightToTreatmentTypeCreate {
	ids := make([]int, len(r))
	for i := range r {
		ids[i] = r[i].ID
	}
	return rtttc.AddTypeIDs(ids...)
}

// Mutation returns the RightToTreatmentTypeMutation object of the builder.
func (rtttc *RightToTreatmentTypeCreate) Mutation() *RightToTreatmentTypeMutation {
	return rtttc.mutation
}

// Save creates the RightToTreatmentType in the database.
func (rtttc *RightToTreatmentTypeCreate) Save(ctx context.Context) (*RightToTreatmentType, error) {
	var (
		err  error
		node *RightToTreatmentType
	)
	if len(rtttc.hooks) == 0 {
		if err = rtttc.check(); err != nil {
			return nil, err
		}
		node, err = rtttc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*RightToTreatmentTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = rtttc.check(); err != nil {
				return nil, err
			}
			rtttc.mutation = mutation
			node, err = rtttc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(rtttc.hooks) - 1; i >= 0; i-- {
			mut = rtttc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, rtttc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (rtttc *RightToTreatmentTypeCreate) SaveX(ctx context.Context) *RightToTreatmentType {
	v, err := rtttc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (rtttc *RightToTreatmentTypeCreate) check() error {
	if _, ok := rtttc.mutation.TypeName(); !ok {
		return &ValidationError{Name: "TypeName", err: errors.New("ent: missing required field \"TypeName\"")}
	}
	if v, ok := rtttc.mutation.TypeName(); ok {
		if err := righttotreatmenttype.TypeNameValidator(v); err != nil {
			return &ValidationError{Name: "TypeName", err: fmt.Errorf("ent: validator failed for field \"TypeName\": %w", err)}
		}
	}
	return nil
}

func (rtttc *RightToTreatmentTypeCreate) sqlSave(ctx context.Context) (*RightToTreatmentType, error) {
	_node, _spec := rtttc.createSpec()
	if err := sqlgraph.CreateNode(ctx, rtttc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (rtttc *RightToTreatmentTypeCreate) createSpec() (*RightToTreatmentType, *sqlgraph.CreateSpec) {
	var (
		_node = &RightToTreatmentType{config: rtttc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: righttotreatmenttype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: righttotreatmenttype.FieldID,
			},
		}
	)
	if value, ok := rtttc.mutation.TypeName(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: righttotreatmenttype.FieldTypeName,
		})
		_node.TypeName = value
	}
	if nodes := rtttc.mutation.TypeIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   righttotreatmenttype.TypeTable,
			Columns: []string{righttotreatmenttype.TypeColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: righttotreatment.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// RightToTreatmentTypeCreateBulk is the builder for creating many RightToTreatmentType entities in bulk.
type RightToTreatmentTypeCreateBulk struct {
	config
	builders []*RightToTreatmentTypeCreate
}

// Save creates the RightToTreatmentType entities in the database.
func (rtttcb *RightToTreatmentTypeCreateBulk) Save(ctx context.Context) ([]*RightToTreatmentType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(rtttcb.builders))
	nodes := make([]*RightToTreatmentType, len(rtttcb.builders))
	mutators := make([]Mutator, len(rtttcb.builders))
	for i := range rtttcb.builders {
		func(i int, root context.Context) {
			builder := rtttcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*RightToTreatmentTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, rtttcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, rtttcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, rtttcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (rtttcb *RightToTreatmentTypeCreateBulk) SaveX(ctx context.Context) []*RightToTreatmentType {
	v, err := rtttcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
